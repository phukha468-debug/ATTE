📋 Прочитай docs/BOOT.md и docs/RULES.md перед выполнением.

<context>
LLM-Judge успешно оценивает тесты и сохраняет результаты в `test_results`. Теперь нам нужен интерфейс для руководителя (role: 'manager' | 'admin'), чтобы просматривать эти результаты.
Благодаря ранее настроенным политикам RLS, запрос к БД от имени менеджера автоматически вернет только результаты сотрудников его компании (`company_id`). Нам нужно реализовать страницу Дашборда с таблицей результатов и детальным просмотром фидбека от ИИ.

ЗАВИСИМОСТИ: 006_llm-evaluate_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: 
- src/pages/Dashboard.tsx (создать/обновить)
- src/lib/api.ts (добавить запрос результатов)
- src/App.tsx (или роутер - добавить защиту роута)
ТИП: feat, ui
</context>

<task>
1. **API (`src/lib/api.ts`):** - Создай функцию `fetchCompanyResults()`.
   - Запрос: `supabase.from('test_results').select('*, users(full_name, role)').order('created_at', { ascending: false })`.
   - RLS на стороне Supabase сам отфильтрует чужие компании.
2. **UI (`src/pages/Dashboard.tsx`):**
   - Добавь проверку роли: если юзер `employee`, показывай "Доступ запрещен" или редирект на `/tests`.
   - Выведи общую статистику в виде карточек (shadcn Card): "Всего пройдено", "Средний балл компании".
   - Выведи таблицу/список (shadcn Table или список Card) сотрудников: Имя, Балл, Дата.
   - Сделай так, чтобы при клике на сотрудника открывалось модальное окно (Dialog/Sheet из shadcn) или расширялась карточка, где виден подробный `llm_feedback` (анализ и рекомендации от Claude).
3. **Навигация:**
   - Убедись, что после успешной авторизации в `App.tsx` / `Layout.tsx` менеджер попадает на Dashboard, а обычный сотрудник — на Tests.
4. ВЕРИФИКАЦИЯ: 
   - Запусти `npm run dev`.
   - Измени свою роль в таблице `users` (в панели Supabase) на `manager`.
   - Обнови страницу, зайди на Dashboard. Убедись, что видишь результаты теста, который ты прошел на предыдущем шаге.
5. Заполни COMPLETION LOG.
6. Перенеси файл в `tasks/done/`.
</task>

<rules>
- СТИЛЬ: Используй компоненты `Table`, `Card`, `Dialog` (или `Sheet`) из shadcn. Дизайн должен выглядеть как серьезный B2B-инструмент.
- RLS TRUST: Не пиши фильтрацию по `company_id` на фронтенде. Доверься RLS.
- Исполнитель: Claude Code (для быстрой верстки B2B-таблиц).
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- [ ] Функция `fetchCompanyResults` с join таблицы users создана
- [ ] Dashboard.tsx сверстан (статистика + таблица)
- [ ] Просмотр детального фидбека (Dialog/Sheet) работает
- [ ] Ролевой редирект настроен (employee не видит дашборд)

### Верификация
- Менеджер видит результаты: [ ]
- Build passed: [ ]