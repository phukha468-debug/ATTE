📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: Проведена. Заказчик предоставил логи консоли из TMA.
ВЫЯВЛЕНО: Приложение крашится при попытке получить профиль пользователя.
ПЕРВОПРИЧИНА: Ошибка Supabase `42P17: infinite recursion detected in policy for relation "users"`. Политики RLS на таблице `users` (profiles) ссылаются сами на себя, вызывая бесконечный цикл.

ЗАВИСИМОСТИ: нет
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: БД Supabase (SQL Editor) / Файлы миграций (если используются)
ТИП: db, fix
</context>

<task>
1. Проанализируй текущие RLS политики для таблицы `users` в Supabase.
2. Найди политику, которая вызывает рекурсию (обычно это попытка проверить роль пользователя через подзапрос к этой же таблице `users`).
3. Напиши исправленный SQL-код для политик таблицы `users`.
   - Пользователь всегда должен иметь возможность читать СВОЮ запись: `(id = auth.uid())`.
   - Если нужна проверка ролей для доступа к чужим записям, избегай прямых селектов из `users` в самой политике (используй JWT claims, или создай функцию `SECURITY DEFINER`).
4. Предоставь Заказчику точный SQL-запрос для выполнения в `Supabase Dashboard -> SQL Editor`, который удалит старые (зацикленные) политики и создаст новые безопасные.
5. Заполни COMPLETION LOG в конце этого файла.
6. Перенеси этот файл из tasks/todo/ в tasks/done/ после того, как Заказчик подтвердит успешное выполнение SQL-кода.
</task>

<rules>
- СТРОГО: Никаких рекурсивных проверок в RLS. Использовать `auth.uid()` для идентификации.
- Исполнитель: Qwen Code.
- ПРОТОКОЛ ОШИБКИ: Если схема БД неизвестна, первым шагом попроси Заказчика выполнить запрос на показ текущих политик (`SELECT * FROM pg_policies WHERE tablename = 'users';`).
</rules>

---
## COMPLETION LOG

**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- ___

### Изменённые файлы
- SQL запрос выполнен в Supabase.

### Верификация
- [ ] Ошибка `infinite recursion` исчезла из консоли браузера.
- [ ] Профиль пользователя успешно загружается на фронтенде.

### Побочные эффекты / риски
- ___

### Открытые вопросы
- ___