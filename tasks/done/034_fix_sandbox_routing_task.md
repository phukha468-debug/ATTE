📋 Прочитай docs/BOOT.md перед выполнением.

<context>
БАГ: На странице `SimulatorPage.tsx` в карточке брифинга кнопка перехода в чат не работает (ничего не происходит при клике).
UX/UI: Текст на кнопке "Перейти в Sandbox" непонятен конечному пользователю.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/SimulatorPage.tsx, src/App.tsx
ТИП: bugfix, ux
</context>

<task>
1. **Rename Button (UX):**
   - В компоненте `SimulatorPage.tsx` (в карточке выбранного задания) измени текст кнопки с "Перейти в Sandbox" на "Начать экзамен".

2. **Fix Navigation (Routing):**
   - Убедись, что кнопка "Начать экзамен" использует `onClick={() => navigate(\`/sandbox/\${task.id}\`)}` (или аналогичный роут).
   - Если используется `<Link>`, убедись, что путь сформирован верно.

3. **Check Router Registration (`App.tsx`):**
   - Проверь файл `src/App.tsx` (или где у нас настроен React Router).
   - Убедись, что роут для чата зарегистрирован: `<Route path="/sandbox/:taskId" element={<SandboxPage />} />`.

4. **ВЕРИФИКАЦИЯ:**
   - Открыть приложение, зайти в Этап 2.
   - Выбрать роль и получить бриф.
   - Нажать "Начать экзамен" -> должен произойти моментальный переход на страницу с чатом.
</task>

<rules>
- Исполнитель: Claude Code.
</rules>

---
## COMPLETION LOG
**Статус:** _done_
**Дата завершения:** 15.04.2026
**Исполнитель:** Gemini CLI

### Сделано
- **UX Update:** Текст кнопки в `SimulatorPage.tsx` изменен с "Перейти в Sandbox" на "Начать экзамен".
- **Bug Fix:** В `SimulatorPage.tsx` добавлен пропущенный импорт `useNavigate` и инициализирована переменная `navigate`, что восстановило работу кнопки.
- **Verification:** Роут в `App.tsx` проверен, он настроен корректно.

### Верификация
- [x] Кнопка "Начать экзамен" отображается.
- [x] Клик по кнопке перенаправляет на страницу `/sandbox/:taskId`.
- [x] Роутинг в `App.tsx` соответствует ожидаемому.