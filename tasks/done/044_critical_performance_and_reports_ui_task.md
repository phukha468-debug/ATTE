📋 Прочитай docs/BOOT.md перед выполнением.

<context>
КРИТИЧЕСКИЙ БАГ ПРОИЗВОДИТЕЛЬНОСТИ И UX: 
Пользователь жалуется на сильные задержки и "дергание" (layout shifts) при навигации между вкладками (Главная, Тесты, Отчеты). Приложение не ощущается плавным.
Также требуется логическая перестановка виджетов на странице Отчетов.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/App.tsx, src/pages/ReportsPage.tsx, компоненты навигации и хуки запросов к БД.
ТИП: performance, ux, refactor
</context>

<task>
1. **Critical Performance Optimization (Anti-Jitter):**
   - **Preloading / Caching:** Убедись, что данные профиля и `test_results` не запрашиваются с нуля при каждом переключении табов. Используй React Context, Zustand или SWR/React Query для кэширования состояния.
   - **Fix Layout Shifts (CLS):** Задай минимальные высоты (`min-h`) для карточек и контейнеров, чтобы при загрузке данных страница не "прыгала". 
   - **Smooth Transitions:** Добавь легкие CSS-транзиции (fade-in) при смене страниц или появлении данных, и используй Skeleton Loaders вместо пустого белого экрана или резких скачков.

2. **ReportsPage.tsx Restructure (UI Logic):**
   - **Блок Этапа 1:** Перемести виджет "Итоговый результат (Этап 1) 80%" (с короной) так, чтобы он находился СРАЗУ ПОД списком оценок (Промптинг, Рутина, Ограничения, Право). Это закроет смысловой блок первого этапа.
   - **Блок Этапа 2:** Оставь как есть (с разбором от Судьи).
   - **Блок Этапа 3:** На место, где раньше был общий результат, добавь новую карточку "Этап 3: Внедрение". Выведи туда данные из таблицы `test_results` (где `type: 'stage3'`). Покажи там сэкономленные часы (ROI) и статус проекта.

3. **ВЕРИФИКАЦИЯ:**
   - Покликать быстро по нижнему меню (Главная -> Тесты -> Отчеты -> Главная). Переключение должно быть мгновенным и без скачков высоты экрана.
   - Проверить страницу Отчетов: структура должна быть строгой (Этап 1 -> Итог 1 -> Этап 2 -> Этап 3).
</task>

<rules>
- Исполнитель: Claude Code.
- Никаких "тяжелых" библиотек анимаций, используй базовый Tailwind (например, `animate-pulse` для скелетонов и `transition-opacity`).
</rules>

---
## COMPLETION LOG
**Статус:** _pending_