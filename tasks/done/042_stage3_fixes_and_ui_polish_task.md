📋 Прочитай docs/BOOT.md перед выполнением.

<context>
БАГИ И UX-ПРАВКИ ФАЗЫ 3:
Пользователь дошел до сдачи Этапа 3. Форма выглядит отлично, калькулятор работает, но есть логические обрывы и UX-несостыковки.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/Stage3Page.tsx, src/pages/TestsPage.tsx, src/pages/HomePage.tsx
ТИП: bugfix, ux
</context>

<task>
1. **Fix DB Insert Error (`Stage3Page.tsx`):**
   - При отправке формы возникает ошибка: `null value in column "company_id" of relation "test_results"`.
   - Убедись, что при сохранении записи `type: 'stage3'`, в payload передаются `user_id` и `company_id` текущего пользователя (аналогично тому, как мы это чинили для Этапа 2).
   - После успешного сохранения должен быть редирект на Главную или красивое сообщение об успехе.

2. **Sync Unlock Logic (`TestsPage.tsx`):**
   - На Главной странице Этап 3 открывается корректно ("Доступно"), но на вкладке "Тесты" он всё ещё висит в статусе "Закрыто" (замок).
   - Перенеси логику разблокировки Этапа 3 (проверка наличия завершенного Этапа 2) из `HomePage` в `TestsPage`.

3. **UX Polish (`HomePage.tsx`):**
   - Удалить большую синюю плавающую кнопку внизу экрана ("Перейти к симулятору" / "Продолжить" и т.д.).
   - Навигация теперь полностью осуществляется через виджет "Путь развития" (клики по самим этапам).
   - Освободившееся место внизу экрана использовать для естественного расширения карточек "Путь развития" или "Советы".
</task>

<rules>
- Исполнитель: Claude Code.
- Если для `Stage3Page` используется отдельный API-эндпоинт, проверь передачу данных и там.
</rules>

---
## COMPLETION LOG
**Статус:** _pending_