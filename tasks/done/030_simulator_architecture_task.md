📋 Прочитай docs/METHODOLOGY_V2.md перед выполнением.

<context>
РЕАЛИЗАЦИЯ ЭТАПА 2 (Симулятор).
Согласно утвержденному плану, симулятор строится на двухуровневой системе профилей (Направление -> Роль) и пуле реалистичных рабочих задач с бенчмарками времени. 
На данном шаге мы создаем структуру данных и UI для выбора роли и получения брифа задания. Сам чат (Sandbox) будет реализован в следующем таске.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/lib/simulatorData.ts (создать), src/pages/SimulatorPage.tsx
ТИП: feat, data-structure
</context>

<task>
1. **Data Structure (`src/lib/simulatorData.ts`):**
   - Создать TypeScript интерфейсы для двухуровневой структуры: `Direction` (направление) и `Role` (роль).
   - Создать интерфейс `SimulatorTask` включающий: `id`, `level` (basic/medium/advanced), `context` (бриф/ситуация), `objective` (что нужно сделать), `format` (формат результата), `benchmarkMinutes` (время без ИИ).
   - Заполнить тестовыми данными 3 направления (например: Продажи, HR, Маркетинг). В каждом по 1-2 роли (например: Продажи -> Менеджер по продажам).
   - Добавить для каждой роли минимум 1 базовую задачу (level: 'basic').

2. **Selection UI (`src/pages/SimulatorPage.tsx`):**
   - ШАГ 1: Экран выбора направления (плитки: Продажи, HR и т.д.).
   - ШАГ 2: После клика на направление -> выбор конкретной роли.
   - UI должен быть компактным, без горизонтального скролла, в стиле обновленной Главной страницы.

3. **Task Briefing UI:**
   - После выбора роли отобразить карточку "Ваша задача".
   - В карточке вывести: Контекст, Задачу, Ожидаемый формат и Бенчмарк времени.
   - Добавить большую кнопку "Перейти в Sandbox (В разработке)", которая пока никуда не ведет.

4. **ВЕРИФИКАЦИЯ:**
   - Открыть страницу Симулятора.
   - Пройти путь: Выбор направления -> Выбор роли -> Просмотр реалистичного брифа задачи.
</task>

<rules>
- СТРОГО: Использовать компактный дизайн из таска 029 (удобные отступы, системные шрифты, без лишней пустоты).
- Данные пока храним хардкодом в `simulatorData.ts` для быстрого прототипирования.
- Исполнитель: Claude Code.
</rules>

---
## COMPLETION LOG
**Статус:** _done_
**Дата завершения:** 15.04.2026
**Исполнитель:** Gemini CLI

### Сделано
- **Data Structure:** Создан файл `src/lib/simulatorData.ts` с интерфейсами `Direction`, `Role` и `SimulatorTask`. Добавлены тестовые данные для Продаж, HR и Маркетинга.
- **Simulator Page:** Реализован пошаговый интерфейс выбора (Направление -> Роль -> Бриф). Использованы анимации `AnimatePresence` для плавных переходов.
- **Task Briefing:** Создана детальная карточка задачи с контекстом, целью, форматом и бенчмарком времени.
- **Navigation:** Добавлен маршрут `/simulator` и обновлена логика кнопки на главной странице (ведет в симулятор, если тест пройден).

### Верификация
- [x] Путь "Выбор направления -> Роль -> Бриф" работает корректно.
- [x] Данные подтягиваются из типизированного хранилища.
- [x] Дизайн соответствует компактному стилю проекта.