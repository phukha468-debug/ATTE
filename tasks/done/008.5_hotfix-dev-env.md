<context>
При локальном запуске проекта (`localhost:3000` / `npm run dev`) мы столкнулись с двумя блокирующими ошибками:
1. Ошибка сборки Vite: `[postcss] @import must precede all other statements`. Глобальный импорт шрифта оказался ниже CSS-переменных.
2. Блокировка авторизации: Объект `window.Telegram.WebApp.initData` отсутствует вне мессенджера, поэтому мы не можем тестировать приложение локально в браузере. Нам нужен безопасный bypass (обход) только для режима разработки.

ЗАВИСИМОСТИ: нет
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: 
- src/index.css (или другой файл с глобальными стилями)
- src/lib/auth.ts
- api/auth/telegram.ts
ТИП: fix, auth
</context>

<task>
1. **CSS Fix (`src/index.css`):**
   - Найди строку `@import url('https://fonts.googleapis.com/css2?family=Inter...');`.
   - Перемести этот импорт на самую первую строчку файла, до блока `:root` и любых других стилей.
2. **Frontend Auth Bypass (`src/lib/auth.ts`):**
   - В функции `loginWithTelegram()` добавь проверку: если мы находимся в режиме разработки (`import.meta.env.DEV === true`) И `initData` пустой, отправляй на бэкенд специальный флаг (например, `initData: "DEV_MODE"`).
3. **Backend Auth Bypass (`api/auth/telegram.ts`):**
   - Если приходит `initData === "DEV_MODE"`, обязательно проверь среду окружения (например, что это не продакшен).
   - Если это безопасно (local dev), пропусти HMAC-валидацию и авторизуй пользователя с моковыми данными (например, `telegram_id: 111222333, first_name: 'Local', last_name: 'Dev'`).
   - Сгенерируй для него JWT через Supabase Admin API точно так же, как для реального юзера, чтобы сессия успешно создалась.
4. ВЕРИФИКАЦИЯ: 
   - Ошибка PostCSS в консоли Vite должна исчезнуть.
   - При открытии `localhost:3000` в обычном браузере (Chrome) пользователь должен успешно проходить авторизацию как "Local Dev" без ошибки "initData недоступен".
5. Заполни COMPLETION LOG и перенеси в `tasks/done/`.
</task>

<rules>
- КРИТИЧНО БЕЗОПАСНОСТЬ: Убедись, что обход авторизации СТРОГО отключен в production, иначе любой сможет зайти под тестовым аккаунтом. Используй надежные проверки среды.
- Исполнитель: Claude Code.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- [ ] CSS @import перенесен наверх
- [ ] Frontend отправляет DEV_MODE флаг при локальном запуске
- [ ] Backend выдает тестовый JWT в обход HMAC
- [ ] Проверка на production добавлена

### Верификация
- Vite запускается без ошибок: [ ]
- Локальный логин работает: [ ]