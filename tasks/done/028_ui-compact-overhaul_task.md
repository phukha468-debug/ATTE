📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: Основная логика работает, данные из БД подтягиваются. UI перегружен лишними элементами и слишком крупными шрифтами, что создает избыточный вертикальный скролл и ошибки верстки (горизонтальный скролл).

ЗАВИСИМОСТИ: 027 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/ReportsPage.tsx, src/pages/HomePage.tsx, src/components/ResultsView.tsx
ТИП: ui, cleanup, refactor
</context>

<task>
1. **Reports Page Cleanup:**
   - ПОЛНОСТЬЮ УДАЛИТЬ: Графики (RadarChart/BarChart), блоки "Экономия времени", "ROI проектов" и блок "Твой результат".
   - НОВАЯ СЕКЦИЯ "Детальные баллы": Вывести список из 4-х категорий (Промптинг, Право, Ограничения, Автоматизация рутины).
   - ФОРМАТ: Название темы — `Балл / Макс. балл` (например: "Промптинг: 7/10"). Использовать данные из `category_scores` в БД.
   - Оформить в виде чистых текстовых строк или компактных карточек.

2. **Home Page Compactness:**
   - Приветствие ("Привет, Имя"): Уменьшить размер шрифта в 3 раза (сделать аккуратным заголовком).
   - Блоки "Навыки" и "Цель": Уменьшить визуальный размер (padding/шрифты) в 2 раза.
   - Виджет "Общий прогресс": Уменьшить высоту и размер на 25%.
   - ЦЕЛЬ: Пользователь должен видеть почти всю важную информацию на одном экране без скролла.

3. **Global Layout Fixes:**
   - ИСКЛЮЧИТЬ горизонтальный скролл (overflow-x-hidden). Все элементы должны строго вписываться в ширину `w-full` / `max-w-screen`.
   - Текст должен автоматически переноситься, кнопки не должны выходить за границы экрана.

4. **ВЕРИФИКАЦИЯ:**
   - Открыть "Отчеты": Виден только текст с баллами по темам и итоговый результат. Всё компактно.
   - Открыть "Главную": Приветствие и блоки стали аккуратными, скролл минимален.
   - Попробовать сжать экран: Ничего не уезжает вправо.
</task>

<rules>
- СТРОГО: Никакой графики Recharts в отчетах. Только текст и CSS-рамки.
- ИСПОЛЬЗОВАТЬ: Системные шрифты (Arial/Helvetica/sans-serif).
- Исполнитель: Claude Code.
</rules>

---
## COMPLETION LOG
**Статус:** _done_
**Дата завершения:** 15.04.2026
**Исполнитель:** Gemini CLI

### Сделано
- **Reports Page:** Полностью удалены графики Recharts. Реализована секция "Детальные баллы" в текстовом формате (Балл/10) для категорий: Промптинг, Автоматизация рутины, Ограничения, Право.
- **Home Page:** Приветствие уменьшено до компактного заголовка. Виджеты "Навыки", "Цель" и "Прогресс" уменьшены в размере (отступы и шрифты) для отображения всей важной информации без скролла.
- **Global Layout:** Добавлен `overflow-x-hidden` в основной Layout. В `src/index.css` шрифты заменены на системные (Arial/Helvetica/sans-serif).
- **Results View:** Экран результатов в `Tests.tsx` также переработан в компактный стиль для визуального единства.

### Верификация
- [x] Отчеты отображаются в текстовом виде, графики Recharts отсутствуют.
- [x] Главная страница стала значительно компактнее, скролл минимален.
- [x] Горизонтальный скролл исключен на уровне глобального Layout.
- [x] Используются системные шрифты.