📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: Авторизация и профиль теперь работают (017, 018). Но UI остается нестабильным.
СИМПТОМЫ: 
1. Нижний тулбар (Tabbar) пропадает при переходе на главную или после начала теста. 
2. Кнопка "Продолжить Аттестацию" не реагирует на нажатия.
3. Кнопки в профиле (Настройки, Выход) — "мертвые".
ПЕРВОПРИЧИНА: Вероятно, состояние навигации (Active Tab) не сохраняется, а Layout-обертка некорректно обрабатывает стейт `authState === 'ready'`.

ЗАВИСИМОСТИ: 017, 018 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/components/Layout.tsx, src/App.tsx, src/components/Navigation.tsx
ТИП: ui, fix
</context>

<task>
1. **Fix Tabbar Visibility:** Убедись, что компонент навигации (Tabbar) отображается всегда, когда пользователь авторизован (`authState === 'ready'`), независимо от текущего роута.
2. **Sync Navigation State:** При нажатии на кнопки тулбара (Главная, Тесты, Профиль) состояние активной вкладки должно четко синхронизироваться с URL.
3. **Continue Button Logic:** Оживи кнопку "Продолжить Аттестацию". Она должна проверять наличие активного `test_id` в `localStorage` или БД и перенаправлять на `/test/:id`.
4. **Profile Buttons Actions:** - Реализуй функцию "Выйти из аккаунта" (supabase.auth.signOut() + очистка стейта).
   - Добавь простые уведомления (Toast) "В разработке" для кнопок Настройки/Безопасность.
5. **ВЕРИФИКАЦИЯ:** - Зайти в приложение -> Нажать "Главная" -> Тулбар не исчез.
   - Нажать "Выйти" в профиле -> Приложение вернуло на экран логина.
   - Нажать "Начать тест" -> Убедиться, что навигация не ломается.
6. Заполнить COMPLETION LOG.
</task>

<rules>
- Не ломать логику загрузки профиля, внедренную в 015.
- Использовать `lucide-react` для иконок, если нужно.
- Исполнитель: Claude Code (Impeccable UI).
</rules>

---
## COMPLETION LOG
**Статус:** _pending_