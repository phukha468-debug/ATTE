📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: БД работает и содержит 15 вопросов. Сетевых ошибок нет. Лог ранее показывал `fetchQuestions: 15 questions loaded`.
СИМПТОМЫ: При переходе к тестам UI показывает сообщения-заглушки: "⚠️ Вопросы не найдены. Запустите: npm run seed" и "⚠️ Вопросы ещё не загружены". Консоль при этом чистая.
ПЕРВОПРИЧИНА: Разрыв состояния на фронтенде (State Management Disconnect). Данные из Supabase успешно фетчатся, но не сохраняются в стейт компонента или глобальный стор (Zustand/Context), из-за чего UI "думает", что массив вопросов пуст.

ЗАВИСИМОСТИ: 020 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/TestsPage.tsx (или компонент теста), src/store/* (если есть), src/lib/api.ts
ТИП: frontend, fix
</context>

<task>
1. **Locate the Disconnect:** Найди в проекте строку `⚠️ Вопросы не найдены. Запустите: npm run seed`. Это компонент, который рендерит тест.
2. **Trace the Data Flow:** Посмотри, откуда этот компонент берет переменную `questions` (из props, хука или стора).
3. **Fix State Binding:** Найди место вызова функции, получающей вопросы из Supabase (скорее всего `fetchQuestions` или `supabase.from('questions').select()`). Убедись, что после получения ответа `data` происходит реальная запись в состояние (например, `setQuestions(data)` или обновление стора).
4. **Remove Placeholder Logic:** Замени хардкодную надпись "npm run seed" на нормальный UI-лоадер, если данные еще грузятся (`isLoading === true`).
5. **ВЕРИФИКАЦИЯ:** - Задеплоить изменения.
   - Нажать "Начать" в разделе тестов.
   - Вместо "Вопросы не найдены" на экране должен отрендериться первый реальный вопрос из базы (например, вопрос про HR-отдел из категории routine).
6. Заполнить COMPLETION LOG.
7. Перенести файл в tasks/done/.
</task>

<rules>
- СТРОГО: Не менять структуру таблицы `questions` в базе данных. 
- Искать проблему только в связывании данных (Data Binding) внутри React.
- Исполнитель: Qwen Code.
</rules>

---
## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- _[что фактически выполнено]_

### Изменённые файлы
- _`src/...`_

### Верификация
- [ ] Ошибка "npm run seed" исчезла.
- [ ] Тест корректно отображает первый вопрос из базы данных.