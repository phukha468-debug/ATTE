📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: Проведена. Заказчик предоставил логи Vercel и Network браузера.
ВЫЯВЛЕНО: Фронтенд зависает в ожидании ответа от `/api/auth/telegram`. Запрос висит (pending).
ПЕРВОПРИЧИНА: Бэкенд Vercel успешно доходит до конца логики (лог: "[auth] ✓ Session generated, returning 200"), но падает с ошибкой `Task timed out after 300 seconds`. Это означает, что главный обработчик роута не возвращает HTTP-ответ (пропущен return) или оставляет висящий незакрытый Promise.

ЗАВИСИМОСТИ: 014, 015, 016 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: api/auth/telegram.ts
ТИП: fix, backend
</context>

<task>
1. Открой `api/auth/telegram.ts`.
2. Найди главный экспортируемый обработчик запроса (например, `export async function POST(req: Request)`).
3. Проверь, как вызывается и возвращается результат основной функции (`authUser` или аналогичной).
4. Убедись, что возвращаемый объект `Response` РЕАЛЬНО передаётся наружу через `return`.
   - ❌ Ошибка: `authUser(supabase, ...); return;`
   - ❌ Ошибка: `const res = await authUser(...); console.log('returning 200'); // конец функции`
   - ✅ Верно: `return await authUser(...);` или `return new Response(...)` в самом конце `POST`.
5. Убедись, что нет "висящих" (unawaited) вызовов к БД (Supabase), которые удерживают event loop открытым.
6. ВЕРИФИКАЦИЯ (поручить Заказчику): Задеплоить, нажать "Вход через Telegram", проверить вкладку Network. Запрос должен завершиться с 200 OK моментально (менее 2-3 секунд).
7. Заполнить COMPLETION LOG.
8. Перенести в tasks/done/.
</task>

<rules>
- СТРОГО: Искать пропущенные `return` в цепочке вызовов (от главного `POST` хендлера до генерации ответа).
- Исполнитель: Qwen Code.
- ПРОТОКОЛ ОШИБКИ: Если `return` везде стоит корректно, проверь импорты Vercel Edge Runtime (иногда требуется `export const runtime = 'edge'`).
</rules>

---
## COMPLETION LOG

**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- ___

### Изменённые файлы
- ___

### Верификация
- [ ] Запрос завершается без таймаута (200 OK).

### Побочные эффекты / риски
- ___

### Открытые вопросы
- ___