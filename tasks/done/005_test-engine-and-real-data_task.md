📋 Прочитай docs/BOOT.md и docs/RULES.md перед выполнением.

<context>
Базовая авторизация и API для получения вопросов готовы. Однако в БД сейчас лежат моковые данные. Заказчик организовал файлы контента в папке `tests/`. Нам нужно:
1. Загрузить реальные вопросы из `tests/STAGE1_QUESTIONS.md`.
2. Реализовать клиентский движок теста на `zustand`, чтобы состояние прохождения (ответы, текущий индекс) сохранялось локально и не терялось при перезагрузке.

ЗАВИСИМОСТИ: 004_seed-and-fetch_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: 
- scripts/seed_questions.ts (обновить пути)
- src/store/testStore.ts (создать)
- src/components/TestRunner.tsx (реализовать логику)
- src/pages/Tests.tsx (интеграция)
ТИП: feat, db
</context>

<task>
1. **Data Update:** - Обнови скрипт `scripts/seed_questions.ts`, чтобы он читал данные из `tests/STAGE1_QUESTIONS.md`. 
   - Учти структуру: домены A, B, C, D. Сохраняй правильные ответы (отмечены как "Оптимальный").
   - Выполни `npm run seed`. Проверь в Supabase, что вопросы обновились.
2. **State Management:**
   - Установи `zustand`. 
   - Создай `src/store/testStore.ts` для хранения: `questions` (массив), `currentIndex`, `answers` (Record<question_id, value>), `isCompleted`.
3. **UI Engine (TestRunner):**
   - Реализуй пошаговое отображение: один вопрос на экран.
   - Поддержка типов: 'mcq' (выбор варианта через RadioGroup/Buttons) и 'open' (ввод текста в Textarea).
   - Анимация переходов между вопросами через `framer-motion` (плавное скольжение или fade).
   - Кнопки: "Назад", "Далее" (или "Завершить" на последнем шаге).
4. **Integration:** - На странице `Tests.tsx` инициализируй store данными из API (`fetchQuestions`) и запускай `TestRunner`.
5. ВЕРИФИКАЦИЯ: 
   - Пройди тест полностью. 
   - Убедись, что на финальном экране в `console.log` выводится объект со всеми ответами, где ключи — это ID из базы.
   - Проверь, что при обновлении страницы (F5) прогресс сохраняется (используй middleware `persist` в Zustand).
6. Заполни COMPLETION LOG.
7. Перенеси файл в `tasks/done/`.
</task>

<rules>
- СТИЛЬ: Чистый shadcn UI. Используй `Progress` компонент для отображения хода теста (например, "Вопрос 3 из 15").
- БЕЗОПАСНОСТЬ: В этом таске мы только собираем ответы. Отправку на сервер и оценку через AI сделаем в следующем.
- Исполнитель: Claude Code (для безупречного UI и стейта).
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- [ ] База данных перенаселена реальными вопросами из tests/STAGE1_QUESTIONS.md
- [ ] Создан Zustand store с персистенцией (localStorage)
- [ ] Реализован TestRunner с поддержкой MCQ и открытых вопросов
- [ ] Добавлены анимации переходов

### Верификация
- Тест проходится до конца: [ ]
- Данные в консоли верны: [ ]
- Build passed: [ ]