📋 Прочитай docs/METHODOLOGY_V2.md перед выполнением.

<context>
Этап 2: Создание LLM-Судьи.
Чат (Sandbox) работает. Теперь нужно реализовать логику оценки. При нажатии на "Завершить задачу" вся история чата отправляется к Claude 3.7 Sonnet, который выступает в роли эксперта-оценщика. Судья анализирует навыки промптинга и возвращает баллы и фидбек.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/SandboxPage.tsx, api/ai/judge.ts (создать), src/pages/SimulatorResultPage.tsx (создать)
ТИП: feat, backend, ai
</context>

<task>
1. **Создание API Судьи (`api/ai/judge.ts`):**
   - Создать Edge-функцию.
   - Подключить OpenRouter (модель: `anthropic/claude-3.7-sonnet` или `anthropic/claude-3-5-sonnet-20241022`, если 3.7 недоступна по API-ключу).
   - Принимает: `task` (бриф задания) и `chatHistory` (массив сообщений).
   - Системный промпт Судьи: "Ты — строгий HR-эксперт по оценке ИИ-навыков. Проанализируй переписку пользователя с ИИ. Оцени навыки промптинга пользователя (дал ли он контекст, роль, формат). Верни строго JSON: { "score": число 0-100, "feedback": "краткий разбор ошибок и плюсов", "time_saved_multiplier": число (во сколько раз ИИ ускорил задачу) }."

2. **UI Результатов (`SimulatorResultPage.tsx`):**
   - Создать новую страницу для отображения вердикта Судьи.
   - Использовать компактный дизайн: Итоговый балл (в рамочке с короной, если > 80), текстовый разбор ошибок (feedback) и метрика "Ускорение задачи" (time_saved_multiplier).
   - Добавить кнопку "Вернуться на Главную".

3. **Связка в Sandbox:**
   - Повесить на кнопку "Завершить задачу" в `SandboxPage.tsx` вызов `/api/ai/judge`.
   - Во время ожидания ответа показывать полноэкранный лоадер "Claude Sonnet анализирует ваши промпты... (может занять до 20 секунд)".
   - После ответа делать редирект на `SimulatorResultPage` с передачей результата.

4. **ВЕРИФИКАЦИЯ:**
   - Провести тестовый диалог в Sandbox.
   - Нажать "Завершить". Дождаться ответа Судьи.
   - Убедиться, что на экране результатов корректно отображается балл и JSON не ломает верстку.
</task>

<rules>
- СТРОГО: Судья должен возвращать только JSON (указать response_format или в промпте жестко запретить markdown-обертки).
- Исполнитель: Claude Code.
</rules>

---
## COMPLETION LOG
**Статус:** _done_
**Дата завершения:** 15.04.2026
**Исполнитель:** Gemini CLI

### Сделано
- **Judge API:** Создана Edge-функция `api/ai/judge.ts` с использованием модели Claude 3.5 Sonnet для глубокого анализа навыков промптинга.
- **Results UI:** Разработана страница `SimulatorResultPage.tsx` с визуализацией баллов, текстовым фидбеком и метрикой ускорения работы.
- **Sandbox Integration:** Реализована отправка истории чата на оценку при нажатии "Завершить задачу". Добавлен полноэкранный лоадер ожидания.
- **Routing:** Зарегистрирован маршрут `/simulator/result` в `App.tsx`.

### Верификация
- [x] Кнопка "Завершить" отправляет данные и показывает лоадер.
- [x] Claude Sonnet успешно возвращает JSON с оценкой и фидбеком.
- [x] Страница результатов корректно отображает данные (балл, текст, множитель).
- [x] Переход на главную после экзамена работает.