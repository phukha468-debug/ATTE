📋 Прочитай docs/BOOT.md перед выполнением.

<context>
КРИТИЧЕСКИЙ БАГ UX: 
Пользователь сообщает, что "дёргание" осталось. При переключении вкладок происходит "двойная загрузка графики" (вероятно, моргают скелетоны, затем данные, либо двойной рендер из-за React Strict Mode / отсутствия глобального кэша).
Также требуется рефакторинг UI на странице Отчетов: объединить разрозненные карточки Этапа 1 в одну.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/ReportsPage.tsx, логика фетчинга данных (хуки/контекст).
ТИП: performance, ui
</context>

<task>
1. **Fix Double Rendering (Performance):**
   - Проанализируй, почему страница моргает. Если данные (профиль, результаты тестов) уже загружены, при переходе на вкладку "Отчеты" они НЕ ДОЛЖНЫ запрашиваться заново с показом лоадера/скелетона. 
   - Используй глобальный стейт (Zustand/Context) или настрой кэширование (например, `staleTime: Infinity` в React Query/SWR), чтобы мгновенно отдавать данные из памяти. Убери лишние `useEffect`, которые триггерят перерисовку.

2. **Merge Stage 1 Cards (`ReportsPage.tsx`):**
   - Сейчас на странице есть две отдельные карточки: одна со списком доменов (Промптинг, Рутина и т.д.), вторая — "Итоговый результат 80%" с короной.
   - **Слей их в одну единую карточку (плитку)** под названием "ЭТАП 1: ЗНАНИЯ".
   - Внутри этой карточки: сначала список из 4 тем, затем визуальный разделитель (`<hr/>` или граница), а под ним — крупно Итоговый результат Этапа 1.

3. **ВЕРИФИКАЦИЯ:**
   - Переключение между Главной и Отчетами должно быть АБСОЛЮТНО моментальным, без единого моргания или сдвига пикселей.
   - Этап 1 на странице отчетов должен выглядеть как монолитный блок.
</task>

<rules>
- Исполнитель: Claude Code.
- Фокус на плавность. Если нужно, добавь глобальный провайдер состояния для кэширования профиля.
</rules>

---
## COMPLETION LOG
**Статус:** _pending_