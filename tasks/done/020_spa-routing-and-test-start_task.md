📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: База данных (RLS) исправлена, профиль пользователя успешно загружается (role=employee). 
СИМПТОМЫ: 
1. Ошибка 404 NOT_FOUND при обновлении страницы или при переходе на маршруты типа `/profile` (особенность деплоя SPA на Vercel).
2. Бесконечный лоадер при нажатии кнопки "Начать" в разделе тестов (хотя вопросы в логах загружены).
3. При нажатии "Выйти из аккаунта" происходит переход на несуществующую физическую страницу.

ЗАВИСИМОСТИ: 017, 018, 019 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: vercel.json (создать), src/App.tsx, src/pages/TestsPage.tsx, src/components/Layout.tsx
ТИП: infra, fix
</context>

<task>
1. **Fix Vercel SPA Routing:** Создай в корне проекта файл `vercel.json` со следующим содержимым:
`{ "rewrites": [ { "source": "/api/(.*)", "destination": "/api/$1" }, { "source": "/(.*)", "destination": "/index.html" } ] }`
2. **Fix Logout Logic:** В компоненте профиля или Layout, где реализована кнопка "Выйти", убедись, что после `supabase.auth.signOut()` используется `useNavigate('/')` для возврата на главную. Исключи использование `window.location.href`, которое вызывает 404.
3. **Unblock Test Start:** - Проверь обработчик кнопки "Начать" в `TestsPage.tsx`. 
   - Убедись, что после нажатия происходит переход на роут конкретного теста (например, `/test/active`).
   - Если используется `useEffect` для ожидания данных, добавь проверку на существование профиля и загруженных вопросов, чтобы лоадер скрывался вовремя.
4. **ВЕРИФИКАЦИЯ:** - Задеплоить проект. 
   - Находясь на странице профиля, нажать F5 — страница должна перезагрузиться корректно (не 404).
   - Нажать "Выйти" — приложение должно вернуться на экран входа.
   - Нажать "Начать" в тестах — должен открыться первый вопрос.
5. Заполнить COMPLETION LOG в конце этого файла.
6. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- СТРОГО: Не менять логику Supabase Auth (она стабильна).
- Использовать ТОЛЬКО `useNavigate` из `react-router-dom` для навигации.
- КЛЮЧИ: Использовать `anon` ключ для клиентских запросов.
- Исполнитель: Claude Code.
</rules>

---

## COMPLETION LOG

**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- _[что фактически выполнено]_

### Изменённые файлы
- _`vercel.json` — добавлен конфиг SPA_
- _`src/...` — исправлена навигация_

### Верификация
- [ ] Ошибка 404 при обновлении страницы исчезла
- [ ] Кнопка "Начать тест" переводит к вопросам
- [ ] Выход из аккаунта работает корректно

### Побочные эффекты / риски
- _[возможные риски]_

### Открытые вопросы
- _[что осталось доделать]_