📋 Прочитай docs/BOOT.md перед выполнением.

<context>
КРИТИЧЕСКИЙ БАГ БЭКЕНДА: При попытке отправить сообщение в чате Песочницы (Sandbox), пользователь мгновенно получает ошибку фронтенда "Ошибка связи с ИИ. Попробуйте еще раз."
ПРИЧИНА: Эндпоинт `/api/ai/simulate` (или функция вызова OpenRouter) падает с ошибкой, возвращает 500, либо падает по таймауту.

ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: api/ai/simulate.ts (или аналогичный), src/pages/SandboxPage.tsx
ТИП: bugfix, backend, critical
</context>

<task>
1. **Debug Edge Function (`api/ai/simulate.ts`):**
   - Проверь правильность чтения переменной окружения `OPENROUTER_API_KEY`. (В Edge Runtime иногда требуется особый подход к `process.env`).
   - Убедись, что используется правильное полное имя модели (например, `google/gemini-2.5-flash` или `openai/gpt-3.5-turbo`).
   - Проверь формат отправляемого JSON (роли `system`, `user`, `assistant`).

2. **Error Handling (Backend & Frontend):**
   - Оберни вызов `fetch` к OpenRouter в надежный `try/catch`. 
   - Если OpenRouter возвращает ошибку, бэкенд должен прокидывать её статус и текст на фронтенд (а не просто падать).
   - В `SandboxPage.tsx` в блоке `catch` выведи реальную ошибку в `console.error()`, чтобы её можно было прочитать в DevTools (F12).

3. **Check Judge API (`api/ai/judge.ts`):**
   - Сделай аналогичную проверку и добавь обработку ошибок для эндпоинта Судьи (чтобы он не упал следом по той же причине).

4. **ВЕРИФИКАЦИЯ:**
   - Написать тестовое сообщение в Sandbox и получить успешный ответ от ИИ.
</task>

<rules>
- Исполнитель: Claude Code.
- Если проблема в локальном запуске (отсутствует .env) — сообщить об этом.
</rules>

---
## COMPLETION LOG
**Статус:** _pending_