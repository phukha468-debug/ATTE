📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик применил строгие RLS-политики (ADR-002). Таблица `questions` пуста. Нам нужно написать утилиту для загрузки (сидирования) вопросов из документации в базу данных Supabase, а затем создать клиентский сервис для их получения.

ЗАВИСИМОСТИ: 003_docs-sync_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: scripts/seed_questions.ts (создать), src/lib/api.ts (создать), package.json
ТИП: feat, db
</context>

<task>
1. Проанализируй файл `STAGE1_QUESTIONS.md`. Там лежат вопросы доменов A, B, C, D.
2. Создай Node.js скрипт `scripts/seed_questions.ts` (используй `tsx` или `ts-node` для запуска). 
3. Скрипт должен: 
   - Инициализировать Supabase с `SUPABASE_SERVICE_ROLE_KEY` (т.к. RLS запрещает INSERT для anon).
   - Заполнить таблицу `questions` минимум 10 вопросами из файла (распарсить или захардкодить их структуру в массив объектов внутри скрипта).
   - Поля: `category` (A/B/C/D), `text` (текст вопроса), `type` ('open' | 'mcq'), `options` (JSON массив вариантов, если есть).
4. Добавь команду в `package.json`: `"seed": "tsx scripts/seed_questions.ts"`. Выполни её, чтобы вопросы улетели в БД.
5. Создай файл `src/lib/api.ts`. Напиши там функцию `export const fetchQuestions = async () => { ... }`, использующую обычный `supabase` клиент (с anon ключом) для получения всех вопросов.
6. ВЕРИФИКАЦИЯ: Добавь временный `console.log` или вызов `fetchQuestions` на странице `Tests.tsx`. Запусти приложение, авторизуйся и убедись, что вопросы успешно приходят с сервера (RLS пропускает).
7. Заполни COMPLETION LOG.
8. Перенеси файл в `tasks/done/`.
</task>

<rules>
- КЛЮЧИ: Скрипт `seed_questions.ts` СТРОГО использует `SUPABASE_SERVICE_ROLE_KEY`. Клиентский `api.ts` СТРОГО использует `supabase` из `src/lib/supabase.ts` (anon key).
- ДАННЫЕ: В `STAGE1_QUESTIONS.md` правильный ответ всегда указан словом "Оптимальный: X". Сохрани эту логику в JSON `options` (например, добавив флаг `isCorrect`).
- Исполнитель: Qwen Code (или Claude).
</rules>

---

## COMPLETION LOG

**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- _[что фактически выполнено]_

### Верификация
- [ ] Скрипт seed отработал без ошибок
- [ ] Данные появились в Supabase
- [ ] Фронтенд успешно получает список вопросов через `fetchQuestions`

### Побочные эффекты / риски
- _нет_