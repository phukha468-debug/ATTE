📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ДИАГНОСТИКА: Аттестация работает. Результаты успешно сохраняются в таблицу `test_results` (score=80).
ПЕРВОПРИЧИНА: Страница "Отчеты" (Reports) и виджеты на "Главной" (Home) всё еще отображают демо-данные или не синхронизированы с реальной таблицей результатов.

ЗАВИСИМОСТИ: 025, 026 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/pages/ReportsPage.tsx, src/pages/HomePage.tsx, src/lib/api.ts
ТИП: feat, data-binding
</context>

<task>
1. **Fetch Real Results:** В `api.ts` (или напрямую в компонентах) реализовать запрос к `test_results`, который тянет последнюю завершенную (`is_completed = true`) попытку текущего пользователя.
2. **Bind Home Widgets:** На главной странице в виджете "Ваш статус" или "Последний результат" отобразить реальный `score` из БД вместо хардкода.
3. **Populate Reports:**
   - Заменить статические графики в "Отчетах" на динамические, использующие данные из последней записи в БД.
   - Использовать `category_scores` из поля `llm_feedback` (JSON) для построения RadarChart или BarChart.
4. **Visual Fix (from 026):** Убедиться, что графики имеют `min-height` и не вызывают ошибку `width(-1)` в консоли.
5. **ВЕРИФИКАЦИЯ:** - После прохождения теста зайти в "Отчеты". Там должен отобразиться результат 80% (или актуальный из БД).
   - На "Главной" в статусе должно быть написано "Аттестация пройдена" или "80 баллов".
6. Заполнить COMPLETION LOG.
</task>

<rules>
- СТРОГО: Использовать данные из последнего `test_results` для `auth.uid()`.
- Исполнитель: Claude Code.
</rules>

---
## COMPLETION LOG
**Статус:** _done_
**Дата завершения:** 15.04.2026
**Исполнитель:** Gemini CLI

### Сделано
- В `src/lib/api.ts` добавлена функция `fetchLatestUserResult` для получения последней завершенной попытки текущего пользователя.
- На главной странице (`src/pages/Home.tsx`) виджет "Навыки" теперь отображает реальный балл (`score`) из БД, а шкала прогресса синхронизирована со статусом аттестации.
- На странице аналитики (`src/pages/Reports.tsx`) статические данные заменены на динамические: "Карта компетенций" строится на основе `category_scores` из последнего теста.
- К контейнерам графиков добавлены `min-h-[250px]` и `min-h-[200px]` для предотвращения ошибок рендеринга и обеспечения стабильной верстки.

### Верификация
- [x] Реальные данные из таблицы `test_results` успешно отображаются в интерфейсе.
- [x] Графики отображают актуальные компетенции на основе LLM-оценки.
- [x] Ошибки `width(-1)` отсутствуют благодаря фиксации высоты контейнеров.