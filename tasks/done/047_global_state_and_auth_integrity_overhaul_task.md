📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
КРИТИЧЕСКИЙ БАГ: Утечка данных между аккаунтами (залипание профиля) и нестабильный UI на вкладках "Тарифы" и "Профиль".
ДИАГНОСТИКА: Проведена 16.04.2026. 
ВЫЯВЛЕНО: Глобальный стейт не сбрасывается при смене пользователя. Вкладки Tariffs и Profile не имеют preloading-логики и фиксированной высоты контейнеров.
ПЕРВОПРИЧИНА: Отсутствие механизма очистки (clear state) в auth-провайдере и жестких ключей кэша, привязанных к userId.

ЗАВИСИМОСТИ: 044, 046 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/store/useUserStore.ts (или аналог), src/pages/ProfilePage.tsx, src/pages/TariffsPage.tsx, src/App.tsx
ТИП: hotfix | refactor
</context>

<task>
1. **Auth Integrity (Fix Account Sticking):**
   - Внедрить функцию `resetStore()` или `logout()`, которая полностью очищает данные пользователя в глобальном стейте (Zustand/Context).
   - Если используется React Query/SWR, добавить `userId` в ключи кэширования (например, `['user_profile', userId]`), чтобы данные разных аккаунтов никогда не пересекались.
   - Вызывать полную очистку кэша при инициализации приложения, если обнаружена смена `session.user.id`.

2. **Universal Performance (Tariffs & Profile):**
   - Применить паттерн из таска 044 для страниц Профиля и Тарифов: добавить `min-h-screen` (или фиксированный размер контентной области).
   - Реализовать Skeleton Loaders для этих вкладок, чтобы избежать моргания при первой загрузке.
   - Убедиться, что данные для этих страниц подгружаются в фоне при запуске приложения (pre-fetching).

3. **ВЕРИФИКАЦИЯ:**
   - Сценарий 1: Зайти под Аккаунтом 1 -> Выйти -> Зайти под Аккаунтом 2. Данные Аккаунта 1 должны полностью исчезнуть.
   - Сценарий 2: Быстрое переключение между ВСЕМИ вкладками (5 штук). Ни одна из них не должна «прыгать» или показывать белый экран более 50мс.
   - Заполнить COMPLETION LOG.
</task>

<rules>
- КЛЮЧИ: Использовать только актуальный `userId` из Supabase Auth для всех запросов.
- ASYNC: Навигация только после того, как стейт подтвердил сброс старых данных.
- Исполнитель: Claude Code (требуется безупречная работа со стейтом).
</rules>

---
## COMPLETION LOG
**Статус:** _pending_