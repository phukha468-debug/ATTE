# TASK-013: DB Integrity Guard & Methodology Sync

📋 **Прочитай docs/BOOT.md**. 
**ПРИНЦИП:** "Reality Sync". Код должен быть зеркалом базы данных.
**ПРИЧИНА:** Ошибки 011/012 (tg_id vs telegram_id) выявили отсутствие защиты от рассинхрона схем.

<context>
Мы успешно исправили базовое поле tg_id. Теперь нужно зачистить остальные потенциальные расхождения, внедрить типизацию и обновить правила работы, чтобы исключить такие ошибки в будущем.
ЗАВИСИМОСТИ: 012 (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: 
- api/auth/telegram.ts (backend)
- src/lib/auth.ts (frontend)
- BRIEF.md (docs)
- docs/METHODOLOGY_UPDATES.md (new)
</context>

<task>
1. **Синхронизация BRIEF.md (Law of Truth):**
   - Обнови раздел "Схема БД" в BRIEF.md. Убедись, что там зафиксированы: `tg_id` (int8), `full_name` (text), `company_id` (uuid), `role` (text). Это единственный источник правды.

2. **Backend Hardening (api/auth/telegram.ts):**
   - Внедрить константу `const DB_SCHEMA = { users: { table: 'users', tg_id: 'tg_id', full_name: 'full_name' } }`.
   - Использовать её во всех вызовах `.from().insert()` и `.eq()`.
   - Убедиться, что при создании пользователя роль пишется как `'employee'` (нижний регистр).

3. **Frontend Type Sync (src/lib/auth.ts):**
   - Обновить интерфейс `AuthUser`, чтобы он соответствовал snake_case колонкам БД (full_name, tg_id, company_id).
   - Это гарантирует, что TypeScript выдаст ошибку при попытке использовать camelCase.

4. **Methodology Protection (docs/METHODOLOGY_UPDATES.md):**
   - Создать файл и добавить раздел "SNAKE_EYES PROTOCOL": 
     "Любое обращение к БД требует предварительной сверки с физической схемой. Использование camelCase в именах колонок БД запрещено. Перед началом задач на DB-логику Исполнитель обязан проверить соответствие BRIEF и реальной таблицы."

5. **ВЕРИФИКАЦИЯ:**
   - `npm run build` проходит без ошибок типизации.
   - Тестовая авторизация через TMA: пользователь создается, данные `full_name` и `tg_id` корректно отображаются в Dashboard Supabase.
</task>

<rules>
- СТРОГО: Только snake_case для полей БД.
- СТРОГО: Никаких `any` в интерфейсах пользователя.
- ПРАВИЛО: Если имя поля в коде отличается от BRIEF — приоритет у BRIEF.
</rules>

---
## COMPLETION LOG
**Статус:** pending
**Дата завершения:** ___
**Исполнитель:** ___
**Что сделано:** ___