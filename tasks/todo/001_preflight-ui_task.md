📋 Прочитай docs/BOOT.md перед выполнением этого задания. (Если файла пока нет, игнорируй).

<context>
Совет Моделей утвердил финальную архитектуру (Supabase + React SPA + Vercel Functions).
Прежде чем мы начнем писать логику авторизации (ADR #1), нам необходимо разблокировать сборку фронтенда. В текущем шаблоне 6 файлов UI-компонентов имеют размер 0 байт: `button.tsx`, `card.tsx`, `badge.tsx`, `avatar.tsx`, `progress.tsx`, `tabs.tsx`. Из-за этого `npm run build` выдает ошибки отсутствующих экспортов.

ЗАВИСИМОСТИ: нет
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: src/components/ui/*.tsx, components.json
ТИП: ui, fix
</context>

<task>
1. Проверь наличие и настройки `components.json`.
2. Восстанови 6 пустых компонентов (`button, card, badge, avatar, progress, tabs`) в папке `src/components/ui/`. Используй команду `npx shadcn@latest add <component>` (или `npx shadcn-ui@latest add <component>`). 
3. Если CLI-команда выдает ошибку совместимости, создай базовые версии этих компонентов вручную, используя Radix UI примитивы и утилиту `cn()` из `src/lib/utils.ts`. Они должны экспортировать ожидаемые страницами интерфейсы (например, `Button`, `Card`, `CardContent` и т.д.).
4. ВЕРИФИКАЦИЯ: Выполни `npm run build`. Сборка должна завершиться успешно (зеленый билд), без единой ошибки, связанной с отсутствующими импортами из `src/components/ui/`.
5. Заполни COMPLETION LOG в конце этого файла.
6. Перенеси этот файл из `tasks/todo/` в `tasks/done/`.
</task>

<rules>
- СТИЛЬ И СТЕК: Строго Tailwind CSS. Никакого добавления новых UI-библиотек (MUI, Ant Design и т.д.).
- ФОКУС: В этом таске ЗАПРЕЩЕНО писать логику БД, трогать `supabase.ts` или настраивать роутинг. Только восстановление UI-примитивов до состояния успешного билда.
- ПРОТОКОЛ ОШИБКИ: Если билд падает по другим причинам (не из-за папки ui/), исправь эти импорты, чтобы добиться успешной сборки. Цель таска — "зеленый" билд.
- Исполнитель: Claude Code или Qwen Code.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- _[что фактически выполнено, отличия от постановки]_

### Изменённые файлы
- _`path/to/file` — что изменено (1 строка)_

### Верификация
- [ ] 6 компонентов в `src/components/ui/` больше не пустые
- [ ] Build: `npm run build` — passed

### Побочные эффекты / риски
- _[что может сломаться в результате этой задачи]_

### Открытые вопросы
- _[нерешённые проблемы, требующие follow-up]_